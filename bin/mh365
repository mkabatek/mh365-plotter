#!/bin/sh
# Wrapper: runs the CLI inside the project venv, from any working directory.
HERE=$(cd "$(dirname "$0")/.." && pwd)
exec env PYTHONPATH="$HERE${PYTHONPATH:+:$PYTHONPATH}" \
     "$HERE/.venv/bin/python" -m mh365 "$@"
